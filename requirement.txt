# InSpectra Analytics Platform - Requirements
# Core Data Science Libraries
pandas>=1.5.0
numpy>=1.21.0
polars>=0.18.0
scipy>=1.9.0

# Database Libraries
duckdb>=0.8.0
sqlite3  # Built into Python
psycopg2-binary>=2.9.0  # PostgreSQL (optional)
mysql-connector-python>=8.0.0  # MySQL (optional)
pyodbc>=4.0.0  # SQL Server (optional)

# Machine Learning Libraries
scikit-learn>=1.1.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Data Visualization
plotly>=5.10.0

# Excel Support
openpyxl>=3.0.0
xlrd>=2.0.0

# Additional Data Processing
requests>=2.28.0
beautifulsoup4>=4.11.0  # For web scraping (optional)

# GUI Framework (Built into Python)
tkinter  # Usually comes with Python

# Cross-platform compatibility
packaging>=21.0  # For version comparison
importlib-metadata>=4.0.0; python_version<"3.8"  # Fallback for older Python

# System information (optional but recommended)
psutil>=5.8.0  # For system memory info
distro>=1.7.0; sys_platform=="linux"  # For Linux distribution info

# Optional Performance Libraries
numba>=0.56.0  # For numerical acceleration (optional)
pyarrow>=9.0.0  # For better Parquet support

# Development and Testing (Optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0

# Documentation (Optional)
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0